<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.liyangass1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:configChanges="orientation"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity
            android:name=".RegisterActivity"
            android:label="@string/title_activity_register"
            android:parentActivityName=".ui.login.LoginActivity">
        </activity>

        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/title_activity_register">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".LoggedInPage"
            android:label="@string/title_activity_logged_in_page">
        </activity>

        <activity
            android:name=".PhotoConfirmation"
            android:label="Confirmation">
        </activity>

        <activity
            android:name=".FullScreenImage"
            android:windowSoftInputMode="adjustPan">
        </activity>

        <activity
            android:name=".GlobalView"
            android:label="GloablView">
        </activity>

    </application>

</manifest>